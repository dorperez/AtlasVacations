type VacationType = {
    vacationID: number,
    title: string,
    description: string,
    startDate: string,
    endDate: string,
    price: number,
    imageName: string
}

export default VacationType